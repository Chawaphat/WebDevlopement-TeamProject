<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <!-- Brand -->
      <a class="navbar-brand" href="/">
        <img 
          src="/img/dekteadgamelogo.png" 
          alt="Logo" 
          width="100" 
          height="30" 
          class="d-inline-block align-text-top"
        >
      </a>
      
      <!-- Toggler (hamburger) -->
      <button 
        class="navbar-toggler" 
        type="button" 
        data-bs-toggle="collapse" 
        data-bs-target="#navbarNavAltMarkup" 
        aria-controls="navbarNavAltMarkup" 
        aria-expanded="false" 
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
  
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <!-- ลิงก์ Admin Panel (แสดงได้ทุกจอ) -->
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link fw-bold" href="#">Admin Panel</a>
          </li>
        </ul>
  
        <!-- ลิงก์ Categories และ Product เฉพาะจอเล็ก (mobile) -->
        <ul class="navbar-nav me-auto d-lg-none">
          <li class="nav-item">
            <a class="nav-link" href="/backoffice/categories">Categories</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/backoffice/products">Product</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/backoffice/managehomepage">Manage highlight game</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/backoffice/managenewrelease">Manage new release game</a>
          </li>
          <% if (locals.session && locals.session.email === 'admin@gmail.com') { %>
          <li class="nav-item">
            <a class="nav-link" href="/backoffice/manageusers">Manage Users</a>
          </li>
          <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/backoffice/order_history">Order History</a>
          </li>
        </ul>
  
        <ul class="navbar-nav ms-auto">
          <% if (locals.session && locals.session.userId) { %>
            <li class="nav-item">
              <a tabindex="0" class="btn btn-danger rounded-pill" id="profilePopover" data-bs-toggle="popover" data-bs-placement="bottom"
                  data-bs-content=" <a href='/profile' class='dropdown-item'>Profile</a> <a href='/logout' class='dropdown-item'>Logout</a> ">
                  Admin_Account
              </a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/login">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-person" viewBox="0 0 16 16">
                  <path d="M8 8a3 3 0 1 0 0-6 3 3 0 0 0 0 6m2-3a2 2 0 1 1-4 0 2 2 0 0 1 4 0m4 8c0 1-1 1-1 1H3s-1 0-1-1 1-4 6-4 6 3 6 4m-1-.004c-.001-.246-.154-.986-.832-1.664C11.516 10.68 10.289 10 8 10s-3.516.68-4.168 1.332c-.678.678-.83 1.418-.832 1.664z" />
                </svg> Login
              </a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <script>
    document.addEventListener('DOMContentLoaded', function () {
        const profilePopover = new bootstrap.Popover(document.getElementById('profilePopover'), {
            html: true,  // Allow HTML content inside the popover
            trigger: 'click'  // Set the trigger to click
        });
  
        // Close popover when clicking anywhere outside of it
        document.addEventListener('click', function (event) {
            const popover = document.getElementById('profilePopover');
            if (!popover.contains(event.target)) {
                const bsPopover = bootstrap.Popover.getInstance(popover);
                if (bsPopover) {
                    bsPopover.hide();
                }
            }
        });
    });
  </script>